#include "../includes/minishell.h"

int	ft_pwd(void)
{
	// char	*cwd;
	// size_t	i;

	// while (envp[i])
	// {
	// 	if (envp[i])
	// 	i++;
	// }
	// if (!cwd)
	// 	return (ERROR);
	// printf("pwd = %s\n", cwd);
	// free(cwd);
	return (SUCCESS);
}

/*
int main(void)
{
	ft_pwd();
}
*/
